[zram0]
# Swap compresse en RAM, en remplacement total d'une partition d'echange :
# aucune ecriture sur le flash, et un acces bien plus rapide.
zram-size = min(ram / 2, 8192)
compression-algorithm = zstd
swap-priority = 100
