# Artefacts de build
/build/
/out/
# Le shell est compilé sur la machine, dans son propre répertoire.
shell/build/
*.img
*.iso
*.rom
*.deb

# Relevés matériel générés par tools/probe-hardware.sh
/hardware-report-*.md

# Divers
*.log
*.swp
.DS_Store

# Manifeste de sauvegarde produit par tools/verify-firmware-backup.sh
# (celui-ci se committe ; le .rom associé, jamais)
!firmware-backup-manifest.txt

# Relevé des touches produit par tools/probe-keys.sh
/touches-relevees.txt

# Binaires des essais du lecteur vidéo (voir shell/essais/construire.sh)
shell/essais/build/

# Sous-produits de tools/fabrique-icones.py. Le thème, lui, SE COMMITTE :
# il est déployé par rootfs/, et une machine fournie ne fait pas tourner le
# générateur.
tools/__pycache__/
/planche-icones.png
